server {
    listen 80;
    server_tokens off;
    client_max_body_size 1G;

    charset utf-8;
    charset_types 
        text/css 
        text/plain 
        text/javascript 
        application/javascript 
        application/json
        text/xml
        application/xml
        application/xml+rss;

    gzip on;
    gzip_disable "msie6";

    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_buffers 16 8k;
    gzip_http_version 1.1;

    gzip_types
        text/plain
        text/css
        text/xml
        application/json
        application/javascript
        application/xml
        application/xml+rss
        application/xhtml+xml
        application/x-font-ttf
        font/opentype
        image/svg+xml
        image/x-icon;

    location /static/ {
        alias /staticfiles/;
        gzip_static on;
        access_log off;
    }

    location /media/ {
        proxy_set_header Host $http_host;
        alias /media/;
    }

    location /dj-admin/ {
        proxy_pass http://backend:8000/dj-admin/;

        # 🔧 Убираем таймауты (делаем их максимально большими)
        proxy_connect_timeout       300s;
        proxy_send_timeout          300s;
        proxy_read_timeout          300s;
        send_timeout                300s;

        # 🧾 Передаём заголовки
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }

    location / {
        proxy_pass http://backend:8000;

        # 🔧 Убираем таймауты (делаем их максимально большими)
        proxy_connect_timeout       300s;
        proxy_send_timeout          300s;
        proxy_read_timeout          300s;
        send_timeout                300s;

        # 🧾 Передаём заголовки
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
}